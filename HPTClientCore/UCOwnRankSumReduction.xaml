<HPT:UCMarkBetControl x:Class="HPTClient.UCOwnRankSumReduction"
             xmlns:HPT="clr-namespace:HPTClient"
            xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
                      x:Name="ucOwnRankSumReduction"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <HPT:UCRacesGrouped MarkBet="{Binding}"></HPT:UCRacesGrouped>

        <GridSplitter BorderBrush="Black" BorderThickness="1" Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Center" ResizeBehavior="BasedOnAlignment" ResizeDirection="Columns" Background="{StaticResource SelectedBrush}" Width="4" Margin="1"/>

        <GroupBox Grid.Column="2" Background="{StaticResource BackgroundBrush}" Name="gbRankvariables" BorderBrush="Black" HorizontalAlignment="Left" HeaderTemplate="{StaticResource GroupBoxHeaderCheckBoxTemplate}">
            <GroupBox.Header>
                <CheckBox x:Name="chkUseHorseRankRule" Margin="3" IsChecked="{Binding Path=ReductionHorseRank}">
                    <TextBlock  HorizontalAlignment="Center" VerticalAlignment="Center" 
                                    FontFamily="Arial" FontSize="12" FontWeight="Bold" Foreground="Black" Text="Använd rankvariabelreducering" />
                </CheckBox>
            </GroupBox.Header>
            
            <ListBox ItemsSource="{Binding HorseOwnRankSumReductionRuleList}">
            <ListBox.ItemTemplate>
                <DataTemplate>

                        <HPT:UCHorseRankSumReductionRule MarkBet="{Binding Path=MarkBet,ElementName=ucOwnRankSumReduction}"/>
                    
                   
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
            
        </GroupBox>
    </Grid>
</HPT:UCMarkBetControl>
