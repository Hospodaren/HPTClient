<UserControl x:Class="HPTClient.UCRaceForOverview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:HPT="clr-namespace:HPTClient"
             mc:Ignorable="d" 
             x:Name="ucRaceForOverview"
             DataContextChanged="UserControl_DataContextChanged"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.Resources>
            <!--<CollectionViewSource x:Key="cvsHorseListOverview" Source="{Binding HorseList}">
                <CollectionViewSource.SortDescriptions>
                    <scm:SortDescription PropertyName="RankOwn"/>
                </CollectionViewSource.SortDescriptions>
            </CollectionViewSource>-->
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Border BorderBrush="Black" BorderThickness="1" CornerRadius="2" Background="LightSkyBlue">
            <StackPanel Orientation="Vertical">
                <TextBlock FontWeight="Bold" Text="{Binding Path=LegNrString}" Margin="1" Padding="1"/>
                <TextBlock Margin="1" Padding="1">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0}{1}">
                            <Binding Path="Distance"/>
                            <Binding Path="StartMethodCode"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </StackPanel>
        </Border>

        <ItemsControl x:Name="icHorseOverview" Grid.Column="1" ItemsSource="{Binding Path=HorseList,ElementName=ucRaceForOverview}">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" ClipToBounds="False"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <!--<ItemsControl.ItemTemplate>
                <DataTemplate>
                    <HPT:UCCompactHorse ClipToBounds="False" 
                                        AllowDrop="True"
                                        MouseMove="icHorseOverview_MouseMove"
                                        DragOver="UCCompactHorse_DragOver"
                                        Drop="UCCompactHorse_Drop"
                                        MouseRightButtonUp="icHorseOverview_MouseRightButtonUp"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>-->
        </ItemsControl>
    </Grid>
</UserControl>
