<UserControl x:Class="HPTClient.UCUserRaceDayInfoCommentsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" MinWidth="400" MinHeight="300">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            
            <!--<StackPanel Orientation="Horizontal">
                <Button x:Name="btnGetCommentsList" Content="Hämta lista" ToolTip="Hämta lista med tillgängliga kommentarer från HPTs server"
                        Click="btnGetCommentsList_Click" Margin="1"/>
                <Button x:Name="btnDownloadSelectedComments" Content="Ladda ner valda" ToolTip="Ladda ner valda kommentarer från HPTs server"
                        Click="btnDownloadSelectedComments_Click" Margin="1"/>
            </StackPanel>-->

            <ListView x:Name="lvwAvailableComments" ItemsSource="{Binding UserCommentsList}" Grid.Row="1">
                <ListView.View>
                    <GridView>

                        <GridViewColumn Header="Användarnamn">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding UserName}" Checked="CheckBox_Checked" Unchecked="CheckBox_Checked"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Beskrivning" DisplayMemberBinding="{Binding Comment}"/>
                        
                    </GridView>
                </ListView.View>
            </ListView>
            
        </Grid>
    </Grid>
</UserControl>
