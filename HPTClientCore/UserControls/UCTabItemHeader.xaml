<UserControl x:Class="HPTClient.UCTabItemHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="ucTabItemHeader">
    <Grid>
        <Grid.Resources>
            
            <Style x:Key="txtHeaderStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="Black"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type TabItem}},Path=IsSelected}" Value="True">
                        <Setter Property="FontWeight" Value="ExtraBold"/>
                        <Setter Property="TextDecorations" Value="Underline"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            
        </Grid.Resources>

        <TextBlock x:Name="txtHeader" Style="{StaticResource txtHeaderStyle}" Text="{Binding Path=Text, ElementName=ucTabItemHeader}" Foreground="{Binding Path=TextColor, ElementName=ucTabItemHeader}">

        </TextBlock>
    </Grid>
</UserControl>
