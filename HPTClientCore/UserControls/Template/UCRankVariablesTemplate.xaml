<UserControl x:Class="HPTClient.UCRankVariablesTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Padding" Value="3"/>
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="MinWidth" Value="60"/>
                        <Setter Property="Height" Value="25"/>
                    </Style>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="BorderBrush" Value="DarkBlue"/>
                        <Setter Property="CornerRadius" Value="1"/>
                    </Style>
                </StackPanel.Resources>

                <Border>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Namn" Margin="2" Foreground="Black" FontSize="12" VerticalAlignment="Center"/>
                        <TextBox x:Name="txtTemplateName" Text="{Binding Name}" Margin="2" Width="200"/>
                    </StackPanel>
                </Border>

                <Border>
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="btnSave" Content="Spara" Click="btnSave_Click" Margin="2"/>
                        <Button x:Name="btnClear" Content="Rensa" Click="btnClear_Click" Margin="2"/>
                        <Button x:Name="btnRemove" Content="Ta bort mall" Click="btnRemove_Click" Margin="2"/>
                    </StackPanel>
                </Border>                
                
            </StackPanel>

            <ListView x:Name="lvwRankVariablesTemplate" ItemsSource="{Binding HorseRankVariableList}" Grid.Row="1">
                <ListView.View>
                    <GridView x:Name="gvwRankVariablesTemplate">

                        <GridViewColumn x:Name="gvcCategory" Header="Kategori">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox x:Name="chkUseVariable" IsChecked="{Binding Use}" Content="{Binding CategoryText}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn x:Name="gvcVariable" Header="Variabel">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock  Text="{Binding Text}" FontWeight="Bold"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn x:Name="gvcWeight" Header="Vikt">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <xctk:DecimalUpDown x:Name="dudWeight" DefaultValue="1.0" Minimum="0.1" Maximum="10.0" Increment="0.1" Value="{Binding Weight}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                    </GridView>
                </ListView.View>
            </ListView>

        </Grid>
    </Grid>
</UserControl>
