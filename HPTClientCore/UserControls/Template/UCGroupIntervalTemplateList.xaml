<UserControl x:Class="HPTClient.UCGroupIntervalTemplateList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:HPT="clr-namespace:HPTClient"
             x:Name="ucGroupIntervalTemplateList"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>

        <Style TargetType="{x:Type ListViewItem}">
            <Setter Property="BorderBrush" Value="Black"></Setter>
            <Setter Property="BorderThickness" Value="0.5"></Setter>
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Margin" Value="1"/>
        </Style>

    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Button x:Name="btnNewGroupIntervalTemplate" Margin="2" Content="Ny gruppintervallmall" Click="btnNewGroupIntervalTemplate_Click" HorizontalAlignment="Left" Width="Auto"/>

            <TabControl x:Name="tcGroupIntervalRulesCollection" TabStripPlacement="Top" 
                        ItemsSource="{Binding Path=Config.GroupIntervalRulesCollectionList,ElementName=ucGroupIntervalTemplateList}" Grid.Row="1">

                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <HPT:UCTabItemHeader Text="{Binding Name}"/>
                    </DataTemplate>
                </TabControl.ItemTemplate>

                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <HPT:UCGroupIntervalTemplate/>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>

        </Grid>
    </Grid>
</UserControl>
