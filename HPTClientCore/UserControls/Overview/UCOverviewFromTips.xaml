<HPT:UCMarkBetControl x:Class="HPTClient.UCOverviewFromTips"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:HPT="clr-namespace:HPTClient"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <ListBox x:Name="lst" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ItemsSource="{Binding Path=RaceDayInfo.RaceList}" ContextMenu="{x:Null}"
                                  Background="{StaticResource BackgroundBrush}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" FontFamily="Arial" FontSize="14" FontWeight="SemiBold" Foreground="Black" Text="{Binding LegNrString}" Grid.Column="0" Padding="2,0,3,0"/>
                        <ItemsControl x:Name="icHorseList" ItemsSource="{Binding HorseList}" Grid.Column="1" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" MinWidth="360">
                            <ItemsControl.ItemTemplate>

                                <DataTemplate>
                                    <DataTemplate.Resources>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="FontFamily" Value="Arial"/>
                                            <Setter Property="FontSize" Value="11"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Scratched}" Value="true">
                                                    <Setter Property="Foreground" Value="WhiteSmoke"/>
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataTemplate.Resources>
                                    <CheckBox Name="chkNrAndPrio" IsChecked="{Binding SelectedFromTip}" Template="{StaticResource DisabledCheckBoxControlTemplate}" ContextMenu="{x:Null}">
                                        <CheckBox.Content>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock x:Name="txtStartNr" Text="{Binding StartNr}"/>
                                                <TextBlock Text="{Binding PrioStringFromTips}" FontWeight="Medium"/>
                                            </StackPanel>
                                        </CheckBox.Content>
                                    </CheckBox>
                                </DataTemplate>

                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1">
            <CheckBox x:Name="chkSelect" IsChecked="True" Content="Välj" Margin="5,1" VerticalAlignment="Center"/>
            <CheckBox x:Name="chkClear" IsChecked="True" Content="Rensa först" Margin="5,1" VerticalAlignment="Center"/>
            <!--<CheckBox x:Name="chkSetRank" Content="Ranka"/>-->
            <!--<CheckBox x:Name="chkSetABC" Content="Sätt ABC" Margin="5,1" VerticalAlignment="Center"/>-->
            <Button x:Name="btnApply" Content="Kör" Click="btnApply_Click" Margin="3,1" Padding="2"/>
        </StackPanel>

    </Grid>
</HPT:UCMarkBetControl>
