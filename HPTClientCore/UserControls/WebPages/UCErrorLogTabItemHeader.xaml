<UserControl x:Class="HPTClient.UCErrorLogTabItemHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Image Source="../../Icons/error.ico"  Height="20"/>
            <TextBlock Text="Fellogg" VerticalAlignment="Center" Margin="1" FontSize="14" FontWeight="Bold" Foreground="DarkBlue"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <TextBlock Text="{Binding CouponReader.FileBetType}" VerticalAlignment="Center" Margin="1" FontSize="12" FontWeight="Bold" Foreground="DarkBlue"/>
            <TextBlock Text="{Binding CouponReader.FileDateString}" VerticalAlignment="Center" Margin="1" FontSize="12" Foreground="DarkBlue"/>
        </StackPanel>
        <TextBlock Text="{Binding CouponReader.FileTrackName}" VerticalAlignment="Center" Grid.Row="2" Margin="1" FontSize="12" Foreground="DarkBlue"/>
    </Grid>
</UserControl>
