<UserControl x:Class="HPTClient.UCNotes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Border BorderBrush="Black" BorderThickness="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBox x:Name="txtNotes" MaxLines="50" TextWrapping="Wrap" Width="200" MinHeight="100" Padding="5" Text="{Binding Path=OwnInformation.Comment,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"/>
                
                <!--<StackPanel Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button x:Name="btnSave" Content="Spara" Click="btnSave_Click"/>
                    <Button x:Name="btnCancel" Content="Avbryt" Click="btnCancel_Click"/>
                </StackPanel>-->
                
            </Grid>
        </Border>
    </Grid>
</UserControl>
