<UserControl x:Class="HPTClient.UCCorrectionTabItemHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Image Source="Icons/propertiesORoptions.ico"  Height="20"/>
            <TextBlock Text="Rättning" VerticalAlignment="Center" Margin="1" FontSize="14" FontWeight="Bold" Foreground="Black"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <TextBlock Text="{Binding CouponReader.FileBetType}" VerticalAlignment="Center" Margin="1" FontSize="12" FontWeight="Bold" Foreground="Black"/>
            <TextBlock Text="{Binding CouponReader.FileDateString}" VerticalAlignment="Center" Margin="1" FontSize="12" Foreground="Black"/>
        </StackPanel>
        <TextBlock Text="{Binding CouponReader.FileTrackName}" VerticalAlignment="Center" Grid.Row="2" Margin="1" FontSize="12" Foreground="Black"/>
    </Grid>
</UserControl>
