<UserControl x:Class="HPTClient.UCHomepage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource BackgroundBrush}" BorderBrush="Black" BorderThickness="1">
            <Label>
                <Hyperlink x:Name="hlKubin" NavigateUri="http://www.hpt.nu/nyheter" RequestNavigate="hlKubin_RequestNavigate">Öppna HPTs hemsida i extern webbläsare</Hyperlink>
            </Label>
        </Border>

        <Border  Grid.Row="1">
            <WebBrowser Source="http://www.hpt.nu/nyheter"/>
        </Border>        
        
    </Grid>
</UserControl>
