<UserControl x:Class="HPTClient.UCManual"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Loaded="UserControl_Loaded">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource BackgroundBrush}" BorderBrush="Black" BorderThickness="1">
            <Button x:Name="btGetLatestManualVersion"  Content="Hämta senaste versionen av manualen" Click="btGetLatestManualVersion_Click" Width="650" HorizontalAlignment="Center"/>
        </Border>

        <Border  Grid.Row="1">
            <WebBrowser x:Name="wbManual" Width="650"/>
        </Border>        
        
    </Grid>
</UserControl>
