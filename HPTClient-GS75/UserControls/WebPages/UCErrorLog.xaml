<UserControl x:Class="HPTClient.UCErrorLog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <ListBox ItemsSource="{Binding Path=ErrorLog}" ScrollViewer.VerticalScrollBarVisibility="Auto" BorderThickness="0" Background="{StaticResource BackgroundBrush}" Grid.Row="0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Red" BorderThickness="2">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Message}" Style="{StaticResource tbBigStyle}" Margin="2" TextWrapping="Wrap" Foreground="Red"/>
                            <TextBlock Text="{Binding StackTrace}" Style="{StaticResource tbStyle}" Margin="2" TextWrapping="Wrap"/>
                            <Button x:Name="btnCopy" Click="btnCopy_Click"  Content="Kopiera" Width="75" Height="25"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ListBox>

        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button x:Name="btnCopyAll" Click="btnCopyAll_Click"  Content="Kopiera alla" Width="100" Height="25" Margin="1"/>
            <!--<Button x:Name="btnMailErrorLog" Click="btnMailErrorLog_Click"  Content="Maila felrapport" Width="100" Height="25" Margin="1"/>-->
        </StackPanel>

    </Grid>
</UserControl>
