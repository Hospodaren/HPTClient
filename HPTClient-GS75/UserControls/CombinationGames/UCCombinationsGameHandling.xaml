<HPT:UCCombBetControl x:Class="HPTClient.UCCombinationsGameHandling"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
            xmlns:HPT="clr-namespace:HPTClient" 
                      x:Name="ucCombinationsGameHandling"
             mc:Ignorable="d">
    <Grid>

        <Grid Grid.Row="1" Height="Auto" HorizontalAlignment="Right">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <!--<ColumnDefinition Width="Auto"/>-->
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <GroupBox Background="{StaticResource BackgroundBrush}"  HeaderTemplate="{StaticResource GroupBoxHeaderTemplate}" Header="Fil" Grid.Column="0" HorizontalAlignment="Left" Margin="6,0,0,0" Grid.Row="1" BorderBrush="Black">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Margin="2" Name="btnCreateCoupons" ToolTip="Sparar XML-fil för inlämning till ATG under 'Mina dokument\HPT Travsystem'" Click="btnCreateCoupons_Click">Spara</Button>
                    <Button Grid.Column="1" Margin="2" Name="btnCreateCouponsAs" ToolTip="Sparar XML-fil för inlämning till ATG med valfritt namn, 'Spara'-knappen rekommenderas för bästa funktion i programmet" Click="btnCreateCouponsAs_Click">Spara som</Button>
                    <Button Grid.Column="2" Margin="2" Name="btnCopy" ToolTip="Kopierar information om systemet till klippbordet, använd Ctrl-V för att klistra in i mail eller dokument" Click="btnCopy_Click" IsEnabled="False">Kopiera</Button>
                    <Button Grid.Column="3" Margin="2" Name="btnPrint" ToolTip="Skriver ut information om systemet" Click="btnPrint_Click" IsEnabled="False" >Skriv ut</Button>
                </Grid>
            </GroupBox>

            <GroupBox Background="{StaticResource BackgroundBrush}"  HeaderTemplate="{StaticResource GroupBoxHeaderTemplate}" Header="Kombinationer" HorizontalAlignment="Stretch" BorderBrush="Black">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="{Binding Path=CombBet.NumberOfSelected,ElementName=ucCombinationsGameHandling,StringFormat={}{0} valda kombinationer}" Name="tbNumberOfCombinations" ToolTip="Visar antalet valda kombinationer" Style="{StaticResource tbStyle}" Margin="1"></TextBlock>
                    <TextBlock Grid.Row="1" Text="{Binding Path=CombBet.TotalStake,ElementName=ucCombinationsGameHandling,StringFormat={}{0} kronor satsade}" Name="tbTotalStake" ToolTip="Visar total insats" Style="{StaticResource tbStyle}" Margin="1"></TextBlock>
                </Grid>
            </GroupBox>

            <GroupBox Background="{StaticResource BackgroundBrush}" BorderBrush="Black" HeaderTemplate="{StaticResource GroupBoxHeaderTemplate}" Header="{Binding Path=CombBet.TimeStampString,ElementName=ucCombinationsGameHandling}" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch">

                <Grid HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ComboBox x:Name="cmbUpdateInterval" Text="Uppdateringsintervall" HorizontalAlignment="Stretch" Grid.Column="0" Margin="2" SelectionChanged="cmbUpdateInterval_SelectionChanged">
                        <ComboBoxItem Content="Aldrig" Tag="0"/>
                        <ComboBoxItem Content="Var 3:e minut" Tag="3"/>
                        <ComboBoxItem Content="Var 5:e minut" Tag="5"/>
                        <ComboBoxItem Content="Var 10:e minut" Tag="10"/>
                        <ComboBoxItem Content="Var 15:e minut" Tag="15"/>
                        <ComboBoxItem Content="Var 20:e minut" Tag="20"/>
                        <ComboBoxItem Content="En gång i halvtimmen" Tag="30"/>
                        <ComboBoxItem Content="En gång i timmen" Tag="60"/>
                    </ComboBox>

                    <Button Name="btnUpdate" ToolTip="Uppdatera odds från ATG" Click="btnUpdate_Click" Grid.Column="1">Uppdatera</Button>
                </Grid>

            </GroupBox>

            <GroupBox Background="{StaticResource BackgroundBrush}" BorderBrush="Black" HeaderTemplate="{StaticResource GroupBoxHeaderTemplate}" Header="Målvinst" Grid.Column="1" HorizontalAlignment="Stretch">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <xctk:IntegerUpDown Value="{Binding Path=CombBet.TargetReturn,ElementName=ucCombinationsGameHandling}" Minimum="0" Maximum="1000000" Increment="100" Margin="2"/>

                    <ComboBox x:Name="cmbTargetReturn" Text="Målvinst" HorizontalAlignment="Stretch" Margin="2" 
                              SelectionChanged="cmbTargetReturn_SelectionChanged" Grid.Column="1">
                        <ComboBoxItem Content="100" Tag="100"/>
                        <ComboBoxItem Content="200" Tag="200"/>
                        <ComboBoxItem Content="300" Tag="300"/>
                        <ComboBoxItem Content="500" Tag="500"/>
                        <ComboBoxItem Content="700" Tag="700"/>
                        <ComboBoxItem Content="1000" Tag="1000"/>
                        <ComboBoxItem Content="2000" Tag="2000"/>
                        <ComboBoxItem Content="5000" Tag="5000"/>
                        <ComboBoxItem Content="10000" Tag="10000"/>
                    </ComboBox>

                </Grid>
            </GroupBox>

        </Grid>

    </Grid>
</HPT:UCCombBetControl>
