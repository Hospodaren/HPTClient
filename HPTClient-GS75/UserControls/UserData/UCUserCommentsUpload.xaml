<HPT:UCMarkBetControl x:Class="HPTClient.UCUserCommentsUpload"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:HPT="clr-namespace:HPTClient"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Beskrivning av kommentarerna" FontSize="12" Padding="1"/>

            <TextBox x:Name="txtCommentDescription" Text="{Binding UserCommentsDescription}" MaxLines="12" Width="300" Height="200" Grid.Row="1" Margin="1" AcceptsReturn="True"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2">
                <Button x:Name="btnUploadComments" Content="Ladda upp kommentarer" Click="btnUploadComments_Click" Margin="1"/>
            </StackPanel>
            
        </Grid>
        </Grid>
</HPT:UCMarkBetControl>
