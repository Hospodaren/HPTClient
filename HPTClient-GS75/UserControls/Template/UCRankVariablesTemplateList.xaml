<UserControl x:Class="HPTClient.UCRankVariablesTemplateList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:HPT="clr-namespace:HPTClient"
             mc:Ignorable="d" 
             x:Name="ucRankVariablesTemplate"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>

        <Style TargetType="{x:Type ListViewItem}">
            <Setter Property="BorderBrush" Value="Black"></Setter>
            <Setter Property="BorderThickness" Value="0.5"></Setter>
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Margin" Value="1"/>
        </Style>

    </UserControl.Resources>
    <Grid>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Button x:Name="btnNewRankVariableTemplate" Margin="2" Content="Ny rankvariabelmall" Click="btnNewRankVariableTemplate_Click" HorizontalAlignment="Left" Width="Auto"/>
            
            <!--<StackPanel Orientation="Horizontal">
                <Button x:Name="btnNewRankVariableTemplate" Margin="2" Content="Ny rankvariabelmall" Click="btnNewRankVariableTemplate_Click" HorizontalAlignment="Left" Width="Auto"/>
                <Button x:Name="btnImportRankVariableTemplates" Margin="2" Content="Importera" Click="btnImportRankVariableTemplates_Click" HorizontalAlignment="Left" Width="Auto"/>
                <Button x:Name="btnExportRankVariableTemplates" Margin="2" Content="Exportera" Click="btnExportRankVariableTemplates_Click" HorizontalAlignment="Left" Width="Auto"/>
            </StackPanel>-->

            <TabControl x:Name="tcRaces" TabStripPlacement="Top" ItemsSource="{Binding Path=Config.RankTemplateList,ElementName=ucRankVariablesTemplate}" Grid.Row="1">

                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <HPT:UCTabItemHeader Text="{Binding Name}"/>
                    </DataTemplate>
                </TabControl.ItemTemplate>

                <TabControl.ContentTemplate>
                    <DataTemplate>                        
                        <HPT:UCRankVariablesTemplate/>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>

        </Grid>
    </Grid>
</UserControl>
