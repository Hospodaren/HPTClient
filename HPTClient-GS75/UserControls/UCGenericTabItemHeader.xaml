<UserControl x:Class="HPTClient.UCGenericTabItemHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             x:Name="ucGenericTabItemHeader"
             mc:Ignorable="d">
    <Grid>
        <Grid.Resources>
            <Style x:Key="txtHeaderStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="DarkBlue"/>
            </Style>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!--<Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>-->

        <Image Source="{Binding Path=Image,ElementName=ucGenericTabItemHeader}" Height="28"/>

        <TextBlock Text="{Binding Path=TextBig,ElementName=ucGenericTabItemHeader}" Grid.Column="1"
                   VerticalAlignment="Center" Margin="1" FontSize="18" FontWeight="SemiBold" Foreground="Black"/>

        <Border x:Name="bdrClose" BorderThickness="1" CornerRadius="0" MouseUp="StackPanel_MouseUp" Grid.Column="2" Margin="5,0,0,0">
            <Border.Style>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="BorderBrush" Value="Transparent" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <!--<Setter Property="BorderBrush" Value="Black" />-->
                            <Setter Property="Background" Value="IndianRed" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock x:Name="tbClose" Text="X" TextAlignment="Center" Padding="2" FontFamily="Verdana" FontStretch="UltraExpanded"/>
        </Border>

    </Grid>
</UserControl>
