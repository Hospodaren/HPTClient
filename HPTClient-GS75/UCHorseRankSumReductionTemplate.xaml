<UserControl x:Class="HPTClient.UCHorseRankSumReductionTemplate"
             x:Name="ucHorseRankSumReductionTemplate"
            xmlns:HPT="clr-namespace:HPTClient"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800">
    <Grid>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Padding" Value="3"/>
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="MinWidth" Value="60"/>
                        <Setter Property="Height" Value="25"/>
                    </Style>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="BorderBrush" Value="DarkBlue"/>
                        <Setter Property="CornerRadius" Value="1"/>
                    </Style>
                </StackPanel.Resources>

                <Border>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Namn" Margin="2" Foreground="Black" FontSize="12" VerticalAlignment="Center"/>
                        <TextBox x:Name="txtName" Text="{Binding Name}" Margin="2" Width="200"/>
                        <Button x:Name="btnRemoveTemplate" Content="Ta bort mall" Click="btnRemoveTemplate_Click"/>
                    </StackPanel>
                </Border>

                <Border>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Spelform" Margin="2" Foreground="Black" FontSize="12"/>
                        <ComboBox x:Name="cmbBetTypeCategory" SelectedItem="{Binding TypeCategory}" Width="100" Margin="2"
                          ItemsSource="{Binding Path=Config.BetTypeCategoryList,ElementName=ucHorseRankSumReductionTemplate}"
                          SelectionChanged="cmbBetTypeCategory_SelectionChanged"/>
                    </StackPanel>
                </Border>

                <Border>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <ComboBox x:Name="cmbHorseRankVariable" Width="100" Margin="2"
                          ItemsSource="{Binding Path=Config.HorseRankVariablesToShow,ElementName=ucHorseRankSumReductionTemplate}"
                          SelectionChanged="cmbHorseRankVariable_SelectionChanged"/>
                        <Button x:Name="btnAddVariable" Click="btnAddVariable_Click" Content="Lägg till variabel"/>
                        <Button x:Name="btnRemoveAll" Click="btnRemoveAll_Click" Content="Ta bort alla"/>
                        <Button x:Name="btnClone" Click="btnClone_Click"  Content="Klona"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <ListBox x:Name="lstHorseRankSumReductionRules" ItemsSource="{Binding RankSumReductionRuleList}" Grid.Row="1">
                <ListBox.ItemTemplate>
                    <DataTemplate>

                        <HPT:UCHorseRankSumReductionRuleTemplate Click="btnRemoveRankReductionRule_Click"/>

                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            
        </Grid>

    </Grid>
</UserControl>
