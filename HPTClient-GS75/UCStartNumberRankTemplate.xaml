<UserControl x:Class="HPTClient.UCStartNumberRankTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <GroupBox x:Name="gbSettings" Background="{StaticResource BackgroundBrush}" BorderBrush="Black" Header="{Binding Name}" HeaderTemplate="{StaticResource GroupBoxHeaderTemplate}" Grid.Column="1" HorizontalAlignment="Left" Margin="2,2,6,2">

            <!--<GroupBox.Resources>
                <Style TargetType="{x:Type CheckBox}">
                    <Style.Setters>
                        <Setter Property="Margin" Value="2,2,12,2"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="FontFamily" Value="Tahoma"/>
                        <Setter Property="FontWeight" Value="Medium"/>
                    </Style.Setters>
                </Style>

            </GroupBox.Resources>-->

            <ListBox ItemsSource="{Binding Path=StartNumberRankList}" Height="Auto" Width="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <CheckBox Name="chkNrSelect" IsChecked="{Binding Select}" Template="{StaticResource CheckBoxControlTemplate}" Content="{Binding StartNumber}"/>

                            <xctk:IntegerUpDown Minimum="0" Maximum="15" Value="{Binding Rank}" Grid.Column="1" Margin="1"/>

                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </GroupBox>
    </Grid>
</UserControl>
